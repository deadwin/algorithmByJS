/**104. 二叉树的最大深度
 *
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
import { TreeNode } from "../problems/isSameTree";
/**
 * @param {TreeNode} root
 * @return {number}
 */
export declare var maxDepth: (root: TreeNode) => any;
