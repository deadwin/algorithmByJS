/**
 * @param {number[][]} matrix
 * @param {number} target
 * @return {boolean}
 */
var searchMatrix = function (matrix, target) {
    for (var i = 0; i < matrix.length; i++) {
        let iEnd = matrix[i][matrix[i].length - 1];
        if (target > iEnd) {
        }
        else {
        }
    }
    return null;
};
